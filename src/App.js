import React from 'react';
import { hot } from 'react-hot-loader';

import './App.css';

const App = () => {
  return (
    <div>
      <h1>App testing</h1>
    </div>
  );
};

export default hot(module)(App);
